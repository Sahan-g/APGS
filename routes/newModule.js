const express = require("express");
const router = express.Router();

const newModuleController = require("../controllers/newModuleController");

//router.post("/", newModuleController.handleNewModule);

module.exports = router;
